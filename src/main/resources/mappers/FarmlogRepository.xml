<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.FarmlogRepository">


	<insert id="insertFarmlog">
		INSERT INTO farmlog
		SET
		member_id = #{memberId},
		crop_variety_id = #{cropVarietyId},
		work_type = #{workType},
		activity_type = #{activityType},
		crop_category = #{cropCategory},
		next_schedule = #{nextSchedule},
		work_date = #{workDate},
		work_memo =
		#{workMemo},
		reg_date = NOW(),
		update_date =
		NOW()
	</insert>

	<select id="getFarmlogs"
		resultType="com.example.demo.vo.Farmlog">
		SELECT F.*,f.img_file_name AS imgFileName, M.nickname AS
		extra__writer
		FROM farmlog AS F
		INNER JOIN `member` AS M
		ON F.member_id =
		M.id
		ORDER BY F.id
		DESC
	</select>

	<select id="getLastInsertId" resultType="int">
		SELECT LAST_INSERT_ID();
	</select>

	<insert id="writeFarmlog">
		INSERT INTO farmlog
		(member_id, crop_variety_id,
		work_type_name, agrochemical_name, work_date,
		next_schedule, work_memo,
		img_file_name,
		reg_date)
		VALUES
		(#{loginedMemberId}, #{cropVarietyDbId},
		#{work_type_name},
		#{agrochemical_name}, #{work_date}, #{nextSchedule},
		#{work_memo}, #{img_file_name}, NOW())
	</insert>



	<select id="findByMemberId" resultType="Farmlog">
		SELECT
		f.*,f.img_file_name AS imgFileName,
		v.variety_name
		AS varietyName,
		c.crop_name AS cropName,
		f.next_schedule AS nextSchedule
		FROM farmlog f
		LEFT JOIN crop_variety v ON f.crop_variety_id = v.id
		LEFT JOIN crop c
		ON v.crop_code = c.crop_code
		WHERE f.member_id =
		#{memberId}
		ORDER BY
		f.work_date DESC
	</select>

	<select id="getFarmlogById" resultType="Farmlog">
		SELECT
		f.*,f.img_file_name AS imgFileName,
		c.crop_name AS
		cropName,
		v.variety_name AS varietyName,
		f.next_schedule AS
		nextSchedule,
		m.nickname AS extrawriterName
		FROM farmlog f
		LEFT JOIN
		crop_variety v ON
		f.crop_variety_id = v.id
		LEFT JOIN crop c ON
		v.crop_code = c.crop_code
		LEFT JOIN member m ON f.member_id = m.id
		WHERE f.id = #{id}
	</select>

	<update id="updateFarmlog">
		UPDATE farmlog
		SET
		crop_variety_id =
		#{crop_variety_id},
		work_type_name = #{work_type_name},
		agrochemical_name = #{agrochemical_name},
		work_date = #{work_date},
		next_schedule = #{nextSchedule},
		work_memo = #{work_memo},
		img_file_name = #{imgFileName},
		update_date
		=
		NOW()
		WHERE id = #{id}
		AND
		member_id = #{member_id}
	</update>



</mapper>